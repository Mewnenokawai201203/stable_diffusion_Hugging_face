Đây là một dự án tạo hình ảnh từ mô tả văn bản với model Hugging Face.


Để sử dụng cần import các thư viện cần thiết và add các thư viện vào requirements.txt

pip install flask flask-cors request

pip install requirements.txt
